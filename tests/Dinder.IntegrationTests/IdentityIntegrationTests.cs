using Xunit;

namespace Dinder.IntegrationTests;

/// <summary>
/// Placeholder for integration tests that will use Testcontainers
/// to spin up a real PostgreSQL+PostGIS instance.
/// </summary>
public class IdentityIntegrationTests
{
    [Fact]
    public void Placeholder_TestProject_BuildsSuccessfully()
    {
        // This test exists to verify the test project compiles and runs.
        // Real integration tests will use Testcontainers once the database
        // schema and migrations are in place.
        Assert.True(true);
    }
}
