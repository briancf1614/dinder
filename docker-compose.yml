services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: dinder
      POSTGRES_PASSWORD: dinder123
      POSTGRES_DB: dinder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: